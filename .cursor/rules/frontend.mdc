---
description: Next.js frontend conventions for Elynd
globs: apps/web/**
alwaysApply: false
---

# Frontend Rules (`apps/web`)

Stack: Next.js App Router, React, TypeScript, TanStack Form + Query, Zustand, Better Auth client (Bearer in `localStorage`), Axios, Tailwind CSS **v4**, shadcn/base-nova under `components/ui/`.

Prefer [`.trellis/spec/web/frontend/`](../../.trellis/spec/web/frontend/index.md) for directory and state layering.

## UI priority

1. Prefer existing components under `components/ui/`
2. Install via shadcn CLI when missing
3. Custom components only when the design system cannot meet the need

## Styling

- Tailwind v4 only (`@import "tailwindcss"`, CSS-first theme)
- Prefer semantic theme tokens (`bg-background`, `text-muted-foreground`, etc.) over raw colors

## Layers

| Layer | Location | Pattern |
|-------|----------|---------|
| Routes | `app/**` | thin pages; compose feature modules |
| Features | `features/**` | forms, panels, feature UI |
| Auth client | `lib/auth/` | Better Auth client + token helpers |
| HTTP | `lib/http/` | Axios + Bearer interceptor |
| Server state | `lib/query/` | TanStack Query |
| Client UI state | `stores/` | Zustand |
| Validation | `lib/validations/` | Zod schemas for forms |

## Auth

- Session token: `localStorage` key from `constants` (`AUTH_TOKEN_STORAGE_KEY`)
- Inject `Authorization: Bearer <token>` on Better Auth client `fetchOptions` and Axios
- Do not reintroduce cookie-only session assumptions unless a task explicitly changes auth mode
