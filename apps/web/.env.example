# Optional: absolute public origin for the Next app (defaults to same-origin '').
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required: Hono API origin for Next /api rewrites and logout BFF (server-side only).
API_INTERNAL_URL=http://localhost:3333
