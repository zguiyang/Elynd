# Action: generate

Generate review (re-meet) items for the article below: **cloze** and **sense** only.

Rules:

- Coverage first, count second — typically a few solid items (often up to 8); 3–5 is fine
- Do **not** force a fixed quota
- Mix `cloze` and `sense` when the text supports both
- Cloze options are **English**; sense options are **Simplified Chinese**
- `hintZh` is always short Simplified Chinese
- `sentence` and `focus` stay English and must come from the article

Level guidance:

- `easy` — concrete words; obvious sentence meaning
- `mid` — useful words in context; light paraphrase of sense
- `stretch` — slightly less obvious usage, still answerable from the sentence
