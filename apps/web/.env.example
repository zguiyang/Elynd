# Optional: absolute public origin for the Next app (defaults to same-origin '').
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Nest API origin used by Next rewrites (server-side only).
API_INTERNAL_URL=http://localhost:3336
