import type { ReactNode } from 'react';

/** Immersive learn chrome — no app sidebar (design: Q2 minimal top bar / full read). */
export default function LearnGroupLayout({ children }: { children: ReactNode }) {
  return children;
}
